---
type Todo = {
  title: string;
};

let response = await fetch("https://jsonplaceholder.typicode.com/todos");
let data = await response.json();
---

<main>
  <h1>Todos</h1>
  <ul>
    {data.map((todo: Todo) => <li>{todo.title}</li>)}
  </ul>
</main>
/main>
